using System.Collections;
using Sorting;
using System;

public class BogoSorter : BaseSorter
{
    //Bogo sort is like taking a deck of cards, checking if its in order, if not in order,
    //throw the deck in the air, pick cards up at random and repeat until deck is sorted.
    //Will I regret this? Probably... definitely :O
    //I HATE THIS SORTER

    protected override IEnumerator Sort()
    {
        int nodeCount = nodes.Length; //how many nodes there are
        Node tempNode; //nodes = visual bar 

       
        return null;
    }
}
